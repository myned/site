---
interface Props {
  class: string
  colors: string[]
}

const { colors, class: className, ...rest } = Astro.props
---

{
  colors.map((color) => (
    <span class={className} style={{ background: color }} {...rest}>
      {color}
    </span>
  ))
}
<slot />
